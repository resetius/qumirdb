(query
  (select
    (select-list
      (item as avg_yearly
        (/ (call sum l_extendedprice) 7)
      )
    )
    (from
      (table lineitem)
      (table part)
    )
    (where
      (&& (&& (&& (== p_partkey l_partkey) (== p_brand (: (cast "Brand#45" <named StringView>) <named StringView>)))
          (== p_container (: (cast "MED BAG" <named StringView>) <named StringView>)))
        (< l_quantity
          (subquery scalar (query
        (select
          (select-list
            (item
              (* 0.20000000000000001 (call avg l_quantity))
            )
          )
          (from
            (table lineitem)
          )
          (where
            (== l_partkey p_partkey)
          )
        )
      )
      )))
    )
  )
)
