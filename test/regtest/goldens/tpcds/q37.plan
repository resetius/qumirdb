(rel limit
  (rel sort
    (rel project
      (rel aggregate
        (rel filter
          (rel join
            (rel join
              (rel join (rel source "item") (rel source "inventory") () (inner))
              (rel source "date_dim")
              ()
              (inner))
            (rel source "catalog_sales")
            ()
            (inner))
          (&& (&& (&& (&& (&& (&& (&& (&& (&& (<= 22 i_current_price) (<= i_current_price (+ 22 30))) (== inv_item_sk i_item_sk))
                        (== d_date_sk inv_date_sk))
                      (<= (: (cast "2001-06-02" <named DATE>) <named DATE>) d_date))
                    (<= d_date (+ (: (cast "2001-06-02" <named DATE>) <named DATE>) (: 60 i32))))
                  (|| (|| (|| (== i_manufact_id 678) (== i_manufact_id 964)) (== i_manufact_id 918))
                    (== i_manufact_id 849)))
                (<= 100 inv_quantity_on_hand))
              (<= inv_quantity_on_hand 500))
            (== cs_item_sk i_item_sk)))
        (keys i_item_id i_item_desc i_current_price))
      (i_item_id i_item_id)
      (i_item_desc i_item_desc)
      (i_current_price i_current_price))
    (i_item_id asc nulls-default))
  (limit 100))
